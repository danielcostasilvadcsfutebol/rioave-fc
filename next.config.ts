// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  experimental: {
    // Habilitar quando necessário para Server Actions
    // serverActions: true,
  },
};

export default nextConfig;
